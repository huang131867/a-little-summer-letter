import React from "react";
import ReactDOM from "react-dom/client";
import { GiftExperience } from "../app/GiftExperience";
import "../app/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <GiftExperience />
  </React.StrictMode>,
);
